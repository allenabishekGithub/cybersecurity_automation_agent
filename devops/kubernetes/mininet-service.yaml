apiVersion: v1
kind: Service
metadata:
  name: mininet-sim
  labels:
    app.kubernetes.io/name: mininet-sim
    app.kubernetes.io/part-of: rag-agent
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: mininet-sim
  ports:
    - name: http
      port: 8080
      targetPort: http
